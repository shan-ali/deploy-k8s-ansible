---
- name: multipass-clean-vms
  hosts: all
  #connection: local 

  tasks: 

  - name: list
    shell: ls

  - name: copy network
    ansible.builtin.copy:
      src: 01-controller-network.yaml
      dest: ./01-controller-network.yaml
